# ~/.bashrc

# Point at your external Bash config file
BASH_CONFIG="$HOME/.config/bash/config.bashrc"

# If it exists and is readable, include it
if [ -f "$BASH_CONFIG" ] && [ -r "$BASH_CONFIG" ]; then
  . "$BASH_CONFIG"
fi
